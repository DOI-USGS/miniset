# Docs build toolchain (mkdocs + Material theme). Pinned for reproducible CI
# builds; matches the versions used locally. Install with:
#   pip install -r docs/requirements.txt
mkdocs==1.6.1
mkdocs-material==9.7.7
pymdown-extensions==11.0.1
